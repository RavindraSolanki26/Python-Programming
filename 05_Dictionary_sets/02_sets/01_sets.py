
# sets are used to store multiple items(different datatype) in a single variable

#set doesn't allow duplicate value

#sets are immutable but we can only remove and add new items

#sets are unordered



# s = set() #this is how we create a empty set.

# we can create the set using the set() constructor

# se = set(("rarr","rad",2,5,2.0))
# print(se)

s = {12,62,30,7.8,4551,232,"Ravindra",True};
# print(type(s))
print(len(s))
print(s)